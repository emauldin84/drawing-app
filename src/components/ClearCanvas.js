import React from 'react'

const ClearCanvas = (props) => {

    return (
        <button className='clear-button' onClick={props.handleClearCanvas}>clear</button>
    )

}

export default ClearCanvas